# Caesar Agent Configuration File
# Only includes settings that differ from DEFAULT_CONFIG

# ============================================================================
# AGENT CONFIGURATION
# ============================================================================
Agent:
  name: CaesarExplorer
  draw_fsm: false

# ============================================================================
# CAESAR-SPECIFIC CONFIGURATION
# ============================================================================
CaesarAgent:
  additional_starting_queries: 11
  max_iterations: 240
  save_graph_interval: 50

  use_quick_explore: True
  quick_explore_insights: True
  exploration_llm_config:
    model: deepseek-chat

  # overwrite_role_file: config/role/self_transcend_role.txt
  # adapt_role_file: config/role/self_transcend_insights_v1.txt
  adapt_role: True

ArtifactSynthesizer:
  synthesis_iterations: 12
  synthesis_drafts: 3
  synthesis_merge_artifacts: true
  synthesis_eli5: false
  synthesis_eli5_length: [null, 450]
  synthesis_human_eval: false
  synthesis_max_length: null
  synthesis_iteration_filter: null

# ============================================================================
# LLM CONFIGURATION
# ============================================================================
LLMHandler:
  provider: deepseek
  base_url: https://api.deepseek.com
  model: deepseek-chat

# ============================================================================
# KNOWLEDGE BASE CONFIGURATION
# ============================================================================
AgentMemory:
  enabled: false
  use_graph: false

ChromaClientManager:
  model: deepseek-chat
  # Local, free, no-API-key embeddings (chroma bundled ONNX all-MiniLM-L6-v2).
  embedding_model: all-MiniLM-L6-v2

# ============================================================================
# WEB SEARCH CONFIGURATION
# ============================================================================
BraveSearch:
  # false → Tavily wins when TAVILY_API_KEY is set, then Brave, then DDGS.
  use_ddgs: False

# ============================================================================
# LOGGING CONFIGURATION
# ============================================================================
Logger:
  include_caller_info: "rome"
  level: "info"